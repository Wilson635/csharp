namespace csharp;

using System;

/*
 * Arithmétique  : +, -, *, /, % (binaire)
 *                 +, -, ++, --, (unaire)
 *
 * Affectation   : =, +=, -=, *=, /=, %=
 *
 * Booléen - (In)egalité           : ==, !, <, >, <=, >=
 * Booléen - binaire conditionnel  : &&, ||
 */

class Csharp
{
    static void Main()
    {
        Console.Title = "My First Game";
        /*
            // Déclaration des variables et Types de données.
            Double a = 12.5;
            Double b = 12.5;

            Double c = a + b;

            string text = "wilson";
            string subText = "Steve";

            // Affichage du resultat.
            Console.WriteLine(text + ' ' + subText);
            Console.WriteLine($"{text} {subText}"); // Autre méthode de concaténation.

            Console.WriteLine(a);
            Console.WriteLine(b);
            Console.WriteLine(c);

            // Utilisation des opérateurs.
            bool result = (5 % 2 == 0);
            Console.WriteLine(result);

            // Utilisation des conditions et opérateur ternaire .
            Console.WriteLine("Enter your name: ");
            string? name = Console.ReadLine();

            Console.WriteLine(name == null
                ? "Please enter your name: "
                : Figgle.FiggleFonts.Standard.Render($"Welcome to {name}"));
        */

    }
}